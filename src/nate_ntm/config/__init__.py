"""Configuration models and loaders for nate_ntm.

The `RuntimeConfig` model and related helpers live here and provide
project path, metadata directory, and control API configuration used by
both the runtime daemon and client tooling.

See `specs/001-swarm-runtime-orchestrator/data-model.md` and
`specs/001-swarm-runtime-orchestrator/plan.md` for field definitions.
"""
