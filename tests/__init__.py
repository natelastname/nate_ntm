"""Top-level test package for nate_ntm.

Having ``tests`` as a package ensures that pytest imports test modules
using fully-qualified names like ``tests.unit.api.test_client`` rather
than treating ``unit`` and ``integration`` as top-level packages.
"""
